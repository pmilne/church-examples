How configure your IDE to edit and compile/run the church sources in this project
--------------------------------------------------------------------------------

Download the church SDK from: http://church-lang.org

Place the sdk in the same directory as this README file.

Inside the SDK there should be a directory called ide-configuration, if there is a subdirectory for your IDE you
can look inside that directory for the README.txt file which should contain the specific instructions your IDE.

